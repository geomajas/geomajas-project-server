<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<parent>
		<groupId>org.geomajas</groupId>
		<artifactId>geomajas-face-dojo</artifactId>
		<version>1.5.8-SNAPSHOT</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.geomajas</groupId>
	<artifactId>geomajas-dojo-client-shrinksafe</artifactId>
	<packaging>jar</packaging>
	<name>Geomajas dojo face, client side (custom build)</name>
	<description>Geomajas dojo face, client side (custom build)</description>

	<dependencies>
		<dependency>
			<groupId>org.geomajas</groupId>
			<artifactId>geomajas-dojo-server</artifactId>
			<version>1.5.8-SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>org.geomajas</groupId>
			<artifactId>geomajas-dojo-client</artifactId>
			<version>1.5.8-SNAPSHOT</version>
			<scope>provided</scope>
		</dependency>
	</dependencies>

	<profiles>
		<profile>
			<id>default</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.geomajas</groupId>
						<artifactId>geomajas-maven-dojo</artifactId>
						<version>1.1</version>
						<executions>
							<execution>
								<goals>
									<goal>build</goal>
								</goals>
								<configuration>
									<layerName>geomajas</layerName>
									<localeList>en,fr,nl,pt,sp</localeList>
									<modules>
										<module>
											<groupId>org.geomajas</groupId>
											<artifactId>geomajas-dojo-client</artifactId>
											<version>${project.version}</version>
											<requires>geomajas.geomajas</requires>
											<type>jar</type>
										</module>
									</modules>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<!-- avoids eclipse build -->
		<profile>
			<activation>
				<property>
					<name>m2e.version</name>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.maven.ide.eclipse</groupId>
						<artifactId>lifecycle-mapping</artifactId>
						<version>0.10.0</version>
						<configuration>
							<mappingId>customizable</mappingId>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>

